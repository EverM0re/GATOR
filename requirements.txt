# Core
numpy>=1.24
pyyaml>=6.0
networkx>=3.0
scikit-learn>=1.3

# Ingestion: page rendering, OCR, and PDF handling
pillow>=10.0
pypdf>=4.0
pdf2image>=1.17
pytesseract>=0.3.10

# Router training and the local encoders
torch>=2.1
sentence-transformers>=2.6
transformers>=4.40

# Dataset download
huggingface-hub>=0.23
datasets>=2.20
pyarrow>=15.0

# OpenAI-compatible endpoint for answer generation and judging
openai>=1.30

# Tests
pytest>=8.0

# Optional: mounting experiments (Section 3.2). Not needed to reproduce
# the standalone results.
#   mem0ai>=0.1.0
